#spring:
#  application:
#    name: spring-data-demo-1
#  datasource:
#    password: postgrespw
#    url: jdbc:postgresql://localhost:32768/postgres
#    username: postgres
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true
#    show-sql: true
#    defer-datasource-initialization: true
#    generate-ddl: true
#  sql:
#     init:
#      mode: always
#server:
#  port: 8090


spring:
  servlet:
    multipart:
      max-file-size: 500KB
      max-request-size: 500KB
  application:
    name: property-management-system
  datasource:
    password: 'Rato@.420'
    url: jdbc:mysql://localhost:3306/manage_property
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
    generate-ddl: true
  sql:
    init:
      mode: always
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 90b120e3708af9
    password: "ec32064be0cb21"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8090